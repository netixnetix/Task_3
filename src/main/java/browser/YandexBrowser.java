package browser;

import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;

public class YandexBrowser extends BaseBrowser {

    public YandexBrowser(ChromeDriver driver, String baseURL) {
        super(driver, baseURL);
    }

}
